cd ~/Projects/MacKeyboardiOS

# Add all files
git add .

# Create initial commit
git commit -m "Initial commit: macOS keyboard layout for iOS with hotkeys and special keys"

# View git log
git log --oneline